```

    # coverage testing
    # 1. remove existing item from the list
    # 2. remove existing item which has two quantities
    # 3. try removing a item which is not in the list, expect the exception
    # 4. check the return item value (part of point 1 and 2)
    # 5. calculate_cart_total values, must reflect the 
    #          changed value after removal

    # add_items ( 1, 2, 3)
    #   remove_item(2)
    #   remove_item(2) - throw exception 

    # remove_item(4) - throw exception 


```
